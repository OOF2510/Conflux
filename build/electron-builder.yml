appId: xyz.simp-bot.Conflux
artifactName: ${name}-v${version}_${os}-${arch}.${ext}
asar: true
mac:
  publish:
    - github
  category: public.app-category.entertainment
win:
  publish:
    - github
  target:
    - nsis
linux:
  publish:
    - github
  category: Video
  target:
    - AppImage
    - deb
  icon: linux_icons

electronDownload:
  mirror: https://github.com/castlabs/electron-releases/releases/download/v
